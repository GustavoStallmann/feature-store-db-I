services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password 
      POSTGRES_DB: mydb
    ports: 
      - 5432:5432
    volumes: 
      - pg_volume:/var/lib/postgresql/18/docker

volumes: 
  pg_volume: 
